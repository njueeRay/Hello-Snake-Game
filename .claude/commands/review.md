请对当前所有改动进行代码审查。

执行步骤：
1. 运行 `git diff HEAD` 查看本次改动
2. 读取被改动的文件理解完整上下文
3. 使用 code-reviewer agent 进行全面审查
4. 输出结构化审查报告

重点检查：
- 游戏逻辑正确性（碰撞、边界、状态机）
- Canvas 性能（不必要的重绘）
- 内存泄漏风险（事件监听、动画帧）
- 代码规范符合度（参考 CLAUDE.md）
